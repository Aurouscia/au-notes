# mkdir
创建文件

# cd
进入指定路径文件

# ls
列出当前文件夹下文件

# find

详见： https://www.runoob.com/linux/linux-comm-find.html

`find [路径] [匹配条件] [动作]`
- 路径可不指定，默认值为`.`
- -pattern 指定搜索字符串，支持`*`和`?`，例如`*.log`
- -delete 删除找到的文件（应该先看一遍文件有哪些，再删除）