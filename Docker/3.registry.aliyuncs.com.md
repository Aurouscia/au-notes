# 阿里云的谷歌镜像源

registry.aliyuncs.com/google_containers/xxx:xxx

## 下载后重命名

如果某些应用指名道姓要某个镜像，例如`k8s.gcr.io/xxx:xxx`，那要以别的名字称呼它：

```sh
podman tag registry.aliyuncs.com/google_containers/xxx:xxx k8s.gcr.io/xxx:xxx
```

tag 会为它起一个新名字，但不会重复占用空间

## 特别注意

确保 tag 前后是同一个东西，不要把镜像的名字或版本号变了（自欺欺人）否则会出怪问题