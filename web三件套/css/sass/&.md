# & 

`&` 表示外层规则，可在前面也可在后面

```scss
.backIcon {
  width: 1.25rem;
  height: 1.25rem;
  transition: transform 0.3s ease;

  .backBtn:hover & {
    transform: translateX(-0.25rem);
  }
}
```

上述内层规则表示 `.backBtn:hover .backIcon`