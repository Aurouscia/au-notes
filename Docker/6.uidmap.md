# uidmap

当容器内用户与容器外用户的 id 不一致时，需要通过 uidmap 来指定映射关系

慎用：复杂的映射关系会导致容器启动时卡死 podman（需要设置大量权限）

## 替代方案建议

在容器外创建同 uid 的新用户，并把新用户加入到老用户同一个组里，目录权限设为 775 或 777

在 rootful podman 或 docker 下，容器内外同 uid 就是同一个用户