# Boot

要发出声音，首先要启动“服务器”

使用 cmd+B （Boot）启动服务器

底部黑色的参数变为绿色说明启动成功了

代码中可通过全局变量`s`来访问服务器，例如 `s.boot` 和 `s.quit`

顶部栏的 Server 面板也可以控制服务器的启动和停止

## 播放声音

如果有播放声音的代码，使用 `shift+enter` 或 `cmd+enter` 播放，使用 `cmd+.` 停止（注意输入法）