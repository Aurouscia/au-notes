声明为abstract的函数和类
抽象类不能实例化，抽象类可以包含普通函数和抽象函数
抽象函数只有函数定义没有函数体
抽象类是不完整的模板

virtual函数可以重写也可以不重写
不重写就默认用原来那个
当有1个以上使用默认的子类时使用

abstract函数必须重写(补上函数体)
当每个都不相同时使用

当类中存在抽象函数时，类必须为抽象类

可以用抽象类声明，但不能用抽象类构造(因为不完整)

sealed类：不能被继承
sealed方法：不能被重写