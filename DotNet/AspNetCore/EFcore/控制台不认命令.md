这是由于项目没有配置 dotnet-ef 工具，应按照以下步骤配置：

- 在项目根目录（sln所在目录）下：`dotnet new tool-manifest`
- 安装工具：`dotnet tool install dotnet-ef --version 9.0.10`
    - 注意：必须指定版本号，否则可能会安装与运行时不匹配的最新版本，导致失败
- 提交刚刚操作创建的 .config/dotnet-tools.json 到源码仓库
- 使用前，先确保工具已安装：`dotnet tool restore`（IDE会自动执行）