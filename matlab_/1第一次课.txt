A = rand(10)
生成一个10x10矩阵，填满0-1的随机数

eig(A)
计算该矩阵的特征值(eigenvalues)

help xxx
取得某指令的信息，可打开文档

diag(D)对角线
sort(diag(D))排序对角线


(y拉布达)
(A-yE)X=0，X右特征向量
Y'(A-yE)=0，Y左特征向量
广义特征值
Ax=yBx


结尾加分号：不打出ans
3^2：优先级很高，不用加括号
\反斜杠：反向除法

[1,2,3;4,5,6;7,8,9]
中括号表示矩阵，矩阵中可以用分号表示换行，逗号和空格都行
...：续行符号，把一个巨长的指令写到几排

1.5e-3：1.5乘10的负三次方


变量名字：字母数字下划线，字母开头，分大小写