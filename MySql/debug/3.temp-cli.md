# 临时容器访问测试

## 启动容器

```bash
docker run -it --rm alpine sh
```

在容器内安装 mariadb-client

```sh
apk add mariadb-client
```

使用 mariadb 客户端连接 mysql（两者兼容）

```sh
mariadb -h x.x.x.x -u xxx -p 
```

## 如果遇到证书问题

--ssl-verify-server-cert=0