# Doc

鼠标选中某个类名，`cmd+D`可查看其描述和示例用法

函数和数组一般以`{}`和`[]`的字面量形式存在，但可以通过选中其类名形式（`Function`、`Array`）cmd+D 来查看其文档

# 两种函数调用

Functional 和 receiver，以下

- someObject.someMethod(anArg)
- someMethod(someObject, anArg)

- { SinOsc.ar(440, 0, 0.2) }.play;
- play({ SinOsc.ar(440, 0, 0.2) });

