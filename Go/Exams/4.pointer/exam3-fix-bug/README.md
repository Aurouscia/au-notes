# 指针纠错题

## 题目要求

以下代码存在指针相关的错误，请找出并修复这些问题。

所有代码在 `main.go` 中，修复后程序应该能正确编译运行并输出预期结果。

---

## 任务

1. 修复 `main.go` 中的所有错误
2. 确保程序输出：
   ```
   修改后 num = 100
   Person: {Bob 25}
   ```

---

## 提示

- 注意 nil 指针的处理
- 注意指针的正确使用方式
- 注意值传递和指针传递的区别
