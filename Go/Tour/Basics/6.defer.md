# defer

如果在一个函数调用前面加 defer，虽然它的参数会立即求值，但函数本身会在当前语句块全部运行完后才会被调用 

## defer 的顺序

如果有多个 defer 的函数调用，注意它们是被压进了一个栈里：最后 defer 的函数最先调用。

```go
func main() {
	fmt.Println("counting")

	for i := 0; i < 10; i++ {
		defer fmt.Println(i)
	}

	fmt.Println("done")
}
// 打印结果：done 9 8 7 ... 1
```