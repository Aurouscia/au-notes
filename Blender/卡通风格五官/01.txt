创建一个球体，上下夹两个方体
方体的视图显示改为线框模式
为球体添加布尔修改器，差值模式，快速解算模式

再复制一个没有任何修改器的球体，用作裹缩目标
创建一个空物体，裹缩到该球体上，投影模式y+方向

眼睛的材质，从左到右依次为：
纹理坐标节点的物体(该空物体)连到映射的矢量
一个常量值，连到映射的缩放
映射的输出连接到渐变纹理(球形模式)
渐变纹理的系数输出连接到颜色渐变(常值模式)
其输出连接到RGB混合的系数上

复制一个上述组，混合在一起，可以成为双层
