# 创建主场景

以 Node 而不是 Node2D 创建主场景，因为它本身不需要 2D 功能，将其命名为 Main

使用顶部的实例化按钮，将 Player 添加为其子级

为 Main 添加更多子级：

- Timer (命名为 MobTimer) - 控制 Mob 生成速度
- Timer (命名为 ScoreTimer) - 让分数每秒自增
- Timer (命名为 StartTimer) - 实际启动前给一个延迟
- Marker2D (命名为 StartPosition) - 指示玩家起始位置

设置 Timer 的 `One Shot` 为 true（MobTimer 除外），并设置时间：

- MobTimer: 0.5
- ScoreTimer: 1
- StartTimer: 2

设置 StartPosition 的位置为 240, 450

## mob 生成点

我们希望 mob 生成在屏幕周围一圈，且运动方向朝向屏幕内侧。

为主场景添加一个 Path2D 子节点，命名为 MobPath，作为 mob 源。

可以注意到选中 Path2D 时，视图页面多了几个按钮，启用“网格吸附”和“智能吸附”，然后选中“添加顶点”

以顺时针方向依次点击视口的四个角（顺时针方向的法向才是朝内），最后点击“闭合曲线”

为 Path2D 创建一个 PathFollow2D 子节点，命名为 MobSpawnLocation，它可以沿着 Path 的轨迹移动以及改变自身朝向

## 主场景脚本

添加一个 PackedScene 属性并 Export 它，这可以让我们选择要实例化的 mob 场景

```gd
extends Node

@export var mob_scene: PackedScene
var score
```

```cs
using Godot;

public partial class Main : Node
{
    // Don't forget to rebuild the project so the editor knows about the new export variable.

    [Export]
    public PackedScene MobScene { get; set; }

    private int _score;
}
```

然后，选中主场景，从文件系统中拖动 mob.tscn 到 inspector 的 MobScene

再添加开始和结束游戏处理函数：

```gd
func game_over():
	$ScoreTimer.stop()
	$MobTimer.stop()

func new_game():
	score = 0
	$Player.start($StartPosition.position)
	$StartTimer.start()
```

```cs
public void GameOver()
{
    GetNode<Timer>("MobTimer").Stop(); // 停止生成敌人
    GetNode<Timer>("ScoreTimer").Stop(); // 停止分数增加
}

public void NewGame()
{
    _score = 0;

    var player = GetNode<Player>("Player");
    var startPosition = GetNode<Marker2D>("StartPosition");
    player.Start(startPosition.Position);

    GetNode<Timer>("StartTimer").Start();
}
```

点击 Player 场景，指定它的 Hit 信号连接名为 GameOver 的函数

然后为三个 Timer 分别设置 timeout 信号的处理函数，名称使用默认值（的 PascalCase 版本）

```gd
func _on_score_timer_timeout():
	score += 1

func _on_start_timer_timeout():
	$MobTimer.start()
	$ScoreTimer.start()
```

```cs
private void OnScoreTimerTimeout()
{
    _score++;
}

private void OnStartTimerTimeout()
{
    GetNode<Timer>("MobTimer").Start();
    GetNode<Timer>("ScoreTimer").Start();
}
```

接下来创建 mob 生成 timer 的处理函数：

```gd
func _on_mob_timer_timeout():
	# Create a new instance of the Mob scene.
	var mob = mob_scene.instantiate()

	# Choose a random location on Path2D.
	var mob_spawn_location = $MobPath/MobSpawnLocation
	mob_spawn_location.progress_ratio = randf()

	# Set the mob's position to the random location.
	mob.position = mob_spawn_location.position

	# Set the mob's direction perpendicular to the path direction.
	var direction = mob_spawn_location.rotation + PI / 2

	# Add some randomness to the direction.
	direction += randf_range(-PI / 4, PI / 4)
	mob.rotation = direction

	# Choose the velocity for the mob.
	var velocity = Vector2(randf_range(150.0, 250.0), 0.0)
	mob.linear_velocity = velocity.rotated(direction)

	# Spawn the mob by adding it to the Main scene.
	add_child(mob)
```

```cs
private void OnMobTimerTimeout()
{
    // 使用 MobScene 属性实例化一个 Mob 对象
    Mob mob = MobScene.Instantiate<Mob>();

    // 使用一个 0～1 的随机数作为 ProgressRatio 来随机选取曲线上的位置
    var mobSpawnLocation = GetNode<PathFollow2D>("MobPath/MobSpawnLocation");
    mobSpawnLocation.ProgressRatio = GD.Randf();

    // 获取其“正方向加了90度”（右转90度）的向量
    float direction = mobSpawnLocation.Rotation + Mathf.Pi / 2;

    // Set the mob's position to a random location.
    mob.Position = mobSpawnLocation.Position;

    // Add some randomness to the direction.
    direction += (float)GD.RandRange(-Mathf.Pi / 4, Mathf.Pi / 4);
    mob.Rotation = direction;

    // Choose the velocity.
    var velocity = new Vector2((float)GD.RandRange(150.0, 250.0), 0);
    mob.LinearVelocity = velocity.Rotated(direction);

    // Spawn the mob by adding it to the Main scene.
    AddChild(mob);
}
```

## 人工测试

在 main 的脚本中添加 _Ready 钩子，里面调用 NewGame（立即开始游戏）

将 main.tscn 设为主场景，并启动游戏

预期行为：

- 能使用键盘操控玩家移动
- mob 在屏幕周围以 0.5 秒的间隔生成，并以随机的方向和速度直线运动
- mob 与玩家碰撞后，玩家消失，mob 停止生成