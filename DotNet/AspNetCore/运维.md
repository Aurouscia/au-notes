# 查找bug
- 如果程序有日志，找其日志文件
- 如果程序没有日志，或未能成功启动，搜索`事件查看器`并查看“应用程序”相关的Error记录


# 搬迁文件
远程桌面不稳定，断连容易导致下载文件失败，可以：

1. 将文件打包后放到某网站的`wwwroot`里（注意有后缀名限制，不都能下载，最好压成`.zip`）
2. 如果下载到本地，可以浏览器输入上述zip的路径下载文件
3. 如果搬到另一台服务器，可使用`curl -O <路径>`下载文件（新版windows才有正规curl，旧版应该使用pwsh的下载）

搬完后，可能需要设置目录的读写权限，才能让应用正常工作。